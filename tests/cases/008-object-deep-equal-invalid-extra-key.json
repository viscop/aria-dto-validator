{
  "name": "object deepEqual rejects extra backend keys",
  "userDTO": {
    "networks": [
      {
        "name": "prod-net",
        "zone": "eu"
      }
    ]
  },
  "backendDTO": {
    "allowedNetworks": [
      {
        "name": "prod-net",
        "zone": "eu",
        "id": "net-123"
      }
    ]
  },
  "policy": [
    {
      "type": "object",
      "leftPath": "networks",
      "rightPath": "allowedNetworks",
      "errorMessage": "Selected network must exactly match backend reference."
    }
  ],
  "expected": {
    "valid": false,
    "errorIncludes": ["Selected network must exactly match backend reference."]
  }
}
